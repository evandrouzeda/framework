export default class Field {
    constructor(type, label, placeholder, list) {
        this.type = type;
        this.label = label;
        this.placeholder = placeholder;
        this.list = list;
    }
}

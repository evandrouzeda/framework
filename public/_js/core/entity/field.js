export default class Field {
    constructor(type, label, placeholder, list, extra) {
        this.type = type;
        this.label = label;
        this.placeholder = placeholder;
        this.list = list;
        this.extra = extra;
    }
}

export default class Extra {
    constructor(title, action, controller, extra) {
        this.title = title;
        this.action = action;
        this.controller = controller;
        this.extra = extra;
    }
}
